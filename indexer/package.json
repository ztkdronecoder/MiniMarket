{
  "name": "cortex-indexer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/patch-pglite.js",
    "dev": "ponder dev",
    "dev:base-sepolia": "NETWORK=base_sepolia ponder dev",
    "reset": "rm -rf .ponder",
    "start": "ponder start",
    "codegen": "ponder codegen",
    "build": "ponder codegen"
  },
  "dependencies": {
    "hono": "^4.12.1",
    "ponder": "0.16.3",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
